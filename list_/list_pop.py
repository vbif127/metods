list_pop = """

    #list.pop(i)      Удаляет i-ый элемент и возвращает его. Если индекс не указан, удаляется последний элемент
    
    x = [1, 2, 3, 4]# x имеет тип  list(список)(x type list)

    print(x)# выведется [1, 2, 3, 4]

    x.pop(0)# из списка удалитца элемент с нулевым индексом

    print(x)# выведется [2, 3, 4]
    
"""